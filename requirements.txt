# Core scientific stack
numpy>=1.26
pandas>=2.0
scipy>=1.11
scikit-learn>=1.3
statsmodels>=0.14
matplotlib>=3.7

# Regime detection
hmmlearn>=0.3

# DTW clustering
dtaidistance>=2.3
tslearn>=0.6

# Sticky Bayesian HMM (optional; only needed for `--bayes` / NUTS sweep)
jax>=0.4
jaxlib>=0.4
numpyro>=0.13
arviz>=0.16

# Live global factors (optional; only needed for factor_source="live")
# yfinance>=0.2

# Tests
pytest>=7.4
